spring:
    jpa:
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        hibernate.ddl-auto: update
    datasource:
        url: jdbc:mysql://localhost:5432/proseo?serverTimezone=Europe/Berlin
        username: postgres
        password: sequel09Master
